@model IEnumerable<News.Models.NewsModel>

@{
    ViewData["Title"] = "News";
}

    <h1>News</h1>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    @*<table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Photo)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Context)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.PublishDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Category)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>*@
    <div class="row mt-5 mb-2 justify-content-center">
        @foreach (var item in Model)
        {
            @*<tr>
                    <td>
                        @if (item.Photo != null)
                        {
                            <img alt="" src="@item.Photo" style="height:100px;width:100px;" />
                        }
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Context)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.PublishDate)
                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => item.Category.Name)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>*@

        <div class="col-lg-4 justify-content-center">
            @*col-md-4*@

            <div>
                @if (item.Photo != null)
                {

                    <img alt="" src="@item.Photo" style="height:150px;width:150px;" />
                }
            </div>
      

            <h2>
                @Html.DisplayFor(modelItem => item.Name)
            </h2>
            <p>
                @Html.DisplayFor(modelItem => item.Context)
            </p>
            <p>
                @Html.DisplayNameFor(model => model.PublishDate): @Html.DisplayFor(modelItem => item.PublishDate)
            </p>

            <p>
                @Html.DisplayNameFor(model => model.Category): @Html.DisplayFor(modelItem => item.Category.Name)
            </p>


            <p>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </p>
            <hr />
        </div>

        }
    </div>


@*</tbody>
    </table>*@
